{
  "name": "cds-feature-attachments-build",
  "version": "0.0.0",
  "private": true,
  "description": "Local @sap/cds-dk for cds-maven-plugin build. Not published.",
  "devDependencies": {
    "@sap/cds-dk": "^10"
  }
}
