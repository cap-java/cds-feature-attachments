name: Label issues

permissions: {}

on:
  issues:
    types:
      - opened

jobs:
  label:
    uses: cap-java/.github/.github/workflows/issue.yml@feat/centralized-actions-workflows
    permissions:
      issues: write
